# A1 — do the note's equations match the code?

Generated by `scripts/scratch/a1_equation_audit.py`. Read-only pass;
`sheaf/dynamic_crop.py` is not modified. H2 recompiles the module
source in memory with the calm branch disabled.

## H1 — the scarcity regulariser

Note eq (14) prints r = (F_twin + f)/(F + f), f 'a small regularising
constant'. Code L661-663: shift = 0.05*sum(safety) + max(0,-min(free,twin)).

### wheat
- sum(safety) = 128.8 MMT, so f = 0.05*sum(safety) = **6.44 MMT**
- mean free = 135.6 MMT, so f is 4.8% of mean accessible stock
- second term max(0,-min(free,twin)) is nonzero at 0/144 steps
- tightest step t=4: free=22.6, twin=25.1 MMT; ratio as coded 1.0873 vs unregularised 1.1121 (-2.2%)
- across all steps: mean |bias| 2.2%, max 7.4%

### maize
- sum(safety) = 130.4 MMT, so f = 0.05*sum(safety) = **6.52 MMT**
- mean free = 212.3 MMT, so f is 3.1% of mean accessible stock
- second term max(0,-min(free,twin)) is nonzero at 1/144 steps, max 15.2 MMT
- tightest step t=22: free=-15.2, twin=207.1 MMT; ratio as coded 35.1036 vs unregularised -13.6395 (-357.4%)
- across all steps: mean |bias| 3.4%, max 357.4%

### rice
- sum(safety) = 78.5 MMT, so f = 0.05*sum(safety) = **3.92 MMT**
- mean free = 38.3 MMT, so f is 10.3% of mean accessible stock
- second term max(0,-min(free,twin)) is nonzero at 58/144 steps, max 36.8 MMT
- tightest step t=2: free=-36.8, twin=-36.8 MMT; ratio as coded 1.0073 vs unregularised 0.9992 (+0.8%)
- across all steps: mean |bias| 35.4%, max 129.0%

## H2 — is the reference identity algebra or a branch?

Note §3.6 calls p* = p0 in a matched run 'an algebraic identity, not a
calibrated residual'. Code L666-669 sets p_star = p0 outright when
|free-twin| < 1e-6 and u_anom < 1e-9 and block_frac < 1e-9.

Test: recompile the module with that branch disabled and re-run the
neither-path configuration that assert_twin_identity uses
(use_amis=False, use_shocks=False, use_demand=False, use_industrial=False).
assert_twin_identity tolerates 2% price drift.

### wheat
- p0 = 213.5 $/t
- calm condition |free-twin|<1e-6 holds at 144/144 steps (max |free-twin| = 0.00e+00 MMT)
- price drift with the branch:    0.000%  PASS at the 2% tolerance
- price drift WITHOUT the branch: 25.623%  FAIL at the 2% tolerance
- max |ask - p0| / p0 in the matched run: 30.799% -- the note's step 'with offer prices at p0' requires this be 0

### maize
- p0 = 135.4 $/t
- calm condition |free-twin|<1e-6 holds at 144/144 steps (max |free-twin| = 0.00e+00 MMT)
- price drift with the branch:    0.000%  PASS at the 2% tolerance
- price drift WITHOUT the branch: 34.218%  FAIL at the 2% tolerance
- max |ask - p0| / p0 in the matched run: 31.207% -- the note's step 'with offer prices at p0' requires this be 0

### rice
- p0 = 339.0 $/t
- calm condition |free-twin|<1e-6 holds at 144/144 steps (max |free-twin| = 0.00e+00 MMT)
- price drift with the branch:    0.000%  PASS at the 2% tolerance
- price drift WITHOUT the branch: 19.485%  FAIL at the 2% tolerance
- max |ask - p0| / p0 in the matched run: 28.948% -- the note's step 'with offer prices at p0' requires this be 0

## H3 — is the unmet-demand channel one-sided?

Code L664-665: u0 = unmet_twin[t]; u_anom = max(0, unmet_frac - u0).
So below-twin unmet cannot lower the price. The note writes Delta u as
'the excess of unmet import demand over its twin value' with no truncation.

- wheat: raw (unmet - twin) is negative at 92/144 steps (min -0.5236); truncation is therefore ACTIVE
- maize: raw (unmet - twin) is negative at 59/144 steps (min -0.2942); truncation is therefore ACTIVE
- rice: raw (unmet - twin) is negative at 100/144 steps (min -0.3282); truncation is therefore ACTIVE

## H4 — does p^tr use this step's asks?

Order in _simulate_window: asks recorded at L599 (pre-update), asks
updated L632-636, p_trade computed L650-653 from the updated `ask`.
So p^tr_t is built from q_{i,t}, not q_{i,t-1}. Magnitude of the
distinction, using recorded pre-update asks against shipments:

- wheat: shipment-weighted mean of PRE-update asks ranges 146.9-596.7 $/t; per-step |ask change| median 7.58, p95 28.79 $/t
- maize: shipment-weighted mean of PRE-update asks ranges 68.5-356.7 $/t; per-step |ask change| median 2.72, p95 16.15 $/t
- rice: shipment-weighted mean of PRE-update asks ranges 270.5-949.2 $/t; per-step |ask change| median 11.35, p95 48.02 $/t

The gap is one ask update, so it is small in calm periods and largest
exactly where asks move fastest, i.e. in the crisis windows.

