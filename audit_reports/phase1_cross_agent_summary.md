# Phase 1 Cross-Agent Section (produced at the close of Phase 1 — not yet adjudicated)

Source agents: Agent 1 (mathematical foundations), Agent 2 (game theory), Agent 3 (storage/TWIST-Agrimate lineage). Full reports: `phase1_agent1_math_foundations.md`, `phase1_agent2_game_theory.md`, `phase1_agent3_storage_lineage.md` in this directory.

## 1. Findings independently supported by more than one agent

- **The solver-fallback/acceptance code at `core.py:281–289` is an under-defended failure point, reached from three independent directions by two different agents.** Agent 1 (F3) hit it via an indefinite $M$ (`DCPError` swallowed by a bare `except Exception`); Agent 3 (S10) hit it via globally-infeasible availability (`infeasible` status swallowed the same way); Agent 1 separately (F14) and Agent 3 separately (S10's falsification path) both independently encountered the linear demand system producing *negative prices* as a proximate cause of extreme states. All three converge on the same crash site (`core.py:289`, `TypeError` on `D.value=None`) and the same root cause (accepting solver output without inspecting `prob.status`) — exactly the item CLAUDE.md's core-module map flagged as a verification question. Neither agent's mandate covered `SpatialEquilibrium.solve` as its own subsystem, so this was found only as a byproduct.
- **Producer income in `ExportRestrictionGame._welfare` is evaluated at unshocked baseline production `Q` (`core.py:440`, passing `self.production0`), not realised output `Q·ξ` or post-storage sales.** Found independently by Agent 2 (F9, full game-theory audit, measured effect: ≤one grid-step price move, confirmed README-consistent, classified D/G) and Agent 3 (S13, storage-relevant angle only, explicitly flagged as overlapping the game auditor's remit, confidence deliberately capped at 80% pending reconciliation). Agent 2's fuller analysis resolves the "is this intended" question further than Agent 3 could from its own remit.
- **A pattern across all three reports, not a single finding:** the shipped 3-grain/17-country demo configuration is safe under every mechanism audited (PD margin, grid discretization, storage timing/drift), but each safety margin is a property of the specific prototype numbers rather than of the model's structure, and each degrades or breaks under the exact extensions README advertises (more grains, stronger substitution, a real calibration, a finer policy grid, a longer horizon).

## 2. Apparent disagreements between agents

- None of the three agents reached a factual conclusion that contradicts another's. No agent's numerical reproduction conflicts with another's.
- The closest thing to tension is a **difference in confidence and completeness, not in claim**, on the shared producer-income finding above: Agent 2 treats it as fully resolved at 95–100% confidence (README-consistent convention, effect small, no action recommended ahead of the F3 grid fix), while Agent 3 explicitly withholds full confidence (80%) and defers resolution to the game auditor. This is not a contradiction — it is the intended effect of independent verification surfacing the same code path at different levels of completeness — but the two reports' recommended actions should be reconciled rather than run in parallel.

## 3. Questions requiring Phase 2 investigation

- **No agent directly audited `SpatialEquilibrium.solve` / the market-QP layer as its own subsystem** (CLAUDE.md partition item 2). It was hit incidentally three times (see §1) but never audited head-on — its solver fallback, status-checking, and KKT/complementary-slackness claims (README §2) remain unverified as a primary target. **→ Assigned to Phase 2 Agent 4.**
- **Reconcile the producer-income finding** (Agent 2 F9 / Agent 3 S13): is `production0` at `core.py:440` intended as realised income or as a fixed policy weight? Settle from author intent or README revision, then decide whether `core.py:440` should pass shocked/post-storage output instead. **→ Still open; not directly assigned to any single Phase 2 agent — flag for final adjudication.**
- **Orchestrator & temporal dynamics** (partition item 5) was covered piecemeal — Agent 3 built the full period timeline for storage purposes, Agent 2 covered the game's invocation within `step()` — but the full README §5 sequencing claim was never audited as its own target independent of those two subsystems. **→ Assigned to Phase 2 Agent 5.**
- **Calibration data** (item 6), **data pipelines** (item 7), and **validation methodology** (item 8) were not audited in Phase 1 at all. **→ Assigned to Phase 2 Agents 6 and 7 respectively.**
- **Interaction effects between the recommended fixes are untested.** Four independent one-line fixes are now on the table (F1's geometric-mean symmetrization; F3's grid refinement; S4's probe-price reuse for storage; S7's anomaly-based government trigger). None were tested jointly, and CLAUDE.md's complexity-budget rule implicitly requires checking whether combined changes interact — e.g., does tightening cross-price terms (F1) change the storage price-band drift (S5) or the game's welfare-surface shape (F2's separability, F13's concavity)? **→ Not yet assigned to any agent; candidate for final adjudication or a future Phase 3.**

No disagreements were adjudicated and no fixes were applied — this closed Phase 1.
