\section{Sitting checklist}

\begin{frame}{What is specified, not analysed}
\topic{The crisis-host equations are now written in README \S8 the way the code runs them. This sitting verifies those equations; it does not reopen them as mismatches.}
README \S8 is the specification. The former documentation gaps (lean
window $k=0$, scarcity $\mathrm{shift}$, E0 as shares, $W$ without a
$1.5s$ floor, residual pool, $h_t$, mean-flex $/24$, $p^{\mathrm{tr}}$)
are written there and shown as green banners.

What remains grey is the leftover annual SPE in \texttt{core.py}
(README \S1--\S6), including that diagonal dominance is a construction
device for $M$, not a remaining certificate. Gate~2 cascade is harvest
diversion --- that is in the README, not a red box.

\texttt{verify\_claims.py} re-derives the numbers. It is not a list of
open fights with the code.
\end{frame}

\begin{frame}[label=questions]{What we would like Agrimate colleagues to stress}
\topic{Five questions we want Agrimate colleagues to stress. Not ``is this Deaton--Laroque?''}
Not ``is this Deaton--Laroque?'' --- we have not claimed that.

\begin{enumerate}
\item Is the 24-step ask-dominated market a fair cousin of Agrimate's
      commercial layer, or a different object we should name more
      carefully?
\item Is an offer-cut $\tau$ the right translation of AMIS, versus
      Agrimate's edge shutdown?
\item Is Jacobi isoelastic $\eta$ the substitution you would put on this
      spine, versus a nested CES / AIDS at the country?
\item Is climatology-relative $S/S^{\mathrm{calm}}$ the right Headey
      trigger, or do you want importer procurement and announcements
      before any who-restricts score?
\item Is anything still missing from README \S8 as a displayed
      equation, versus a procedure (LOWESS kernel, Vietnam E0 repair)?
\end{enumerate}
\end{frame}

\begin{frame}{Reproduce}
\topic{How to re-derive every banner from the repo. Does not retune anything.}
From the repository root (after \texttt{pip install -r requirements.txt}):

\begin{center}
\texttt{python3 overleaf/sheaf\_deck/verify\_claims.py}
\end{center}

Re-derives every match banner. Does not retune, does not write
scores, does not unpause \texttt{dynamic\_grains.py}.

Official locked scores remain:

\texttt{diagnostics/gate0\_\{wheat,maize,rice\}\_report.md},
\texttt{gate1\_report.md}, \texttt{gate2\_beta\_report.md}.

Overleaf: zip \texttt{overleaf/sheaf\_deck/} and compile pdfLaTeX + BibTeX.
\end{frame}

\begin{frame}{One-sentence summary}
\topic{One sentence: what the crisis model is, and what the leftover annual code is not.}
\hyperlink{sitmap}{\beamerreturnbutton{sitting map}}
\vspace{0.3em}
SHEAF's crisis model is a 24-step, ask-dominated, single-world-price
market with AMIS offer cuts (Gate~0), Jacobi isoelastic cross-grain
demand on that locked spine (Gate~1, no $\sigma^\star$), and a
Headey-clock climatology-relative $\tau_t$ whose two-player cascade is
harvest diversion (Gate~2 beta). The annual SPE plus year-Nash in
\texttt{core.py} is still in the tree. It is the TWIST leftover. It is
not what we are asking you to certify as 2007/08.
\end{frame}
