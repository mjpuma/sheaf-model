% REPLACE this file when the next experiment is chosen. Do not accumulate
% completed protocols here --- those go into tables/log.tex.
\section{Next experiment (do this)}
\label{sec:next}

\textbf{E8 --- Write the P3 draft}

Handoff: \texttt{diagnostics/GATE1\_HANDOFF.md}. The claim is already
frozen in \S\ref{sec:claim}. Turn it into a short paper in the same
register as \texttt{overleaf/gate0\_whitepaper/}: plain English first,
then equations, then the scored band, E5 leftover, and E6 hold-out.
Cite Agrimate as the single-commodity limit~\citep{kuhla2025}.

Do \textbf{not} start P4--P5. Do \textbf{not} retune. Do \textbf{not}
select \(\sigma^\star\). Optional, not blocking: score world consumption
vs PSD under \(\sigma>0\) as a diagnostic (Gate~0 leftover).
