% REPLACE this file when the next experiment is chosen. Do not accumulate
% completed protocols here --- those go into tables/log.tex.
\section{Next experiment (do this)}
\label{sec:next}

\textbf{Substitution: stop.} The draft is written:
\texttt{overleaf/gate1\_whitepaper/}. Zip that folder for Overleaf.
The frozen claim in \S\ref{sec:claim} is unchanged. Do \textbf{not}
select \(\sigma^\star\), densify \(\{0,0.3,0.6\}\), retune
\texttt{CropParams} or \(\rho\), or unpause
\texttt{sheaf/dynamic\_grains.py}.

\textbf{Do not re-run Gate~0 or this $\sigma$ band} to host the export
game. Headey (2011) is why actions have dates; the game belongs on the
already-locked 24-step spine, with types slow
(\texttt{diagnostics/GAME\_CLOCK.md}). Two-player cascade is in
(Russia harvest, Kazakhstan neighbor). Assessment note:
\texttt{overleaf/gate2\_assessment/}. Further on that layer, if any:
importer scramble or stock announcement --- not a new paper title.
